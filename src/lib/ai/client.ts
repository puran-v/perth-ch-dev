/**
 * Base Claude API client shared across all AI engine modules.
 * Each module (sales, inventory, warehouse, finance) provides its own
 * system prompt and context, but they all go through this function.
 *
 * TODO: Install @anthropic-ai/sdk and implement when AI features are built.
 *
 * @author AI-assisted
 * @created 2026-04-02
 * @module Shared - AI
 */
